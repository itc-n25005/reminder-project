export type Task = {
  id: number;
  event: string;
  date: string;
  time: string;
  done: boolean;
};

export type Todo = {
  id: number;
  event: string;
  date: string;
  time: string;
  done: boolean;
};
