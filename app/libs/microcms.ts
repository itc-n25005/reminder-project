export interface Task {
  id: string;
  event: string;
  date: string;
  time: string;
  done: boolean;
}

export interface Todo {
  id: string;
  event: string;
  date: string;
  time: string;
  done: boolean;
}
